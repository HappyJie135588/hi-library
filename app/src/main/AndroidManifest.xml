<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.happyjie.hi_library">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".demo.fragment.HiFragmentActivity"></activity>
        <activity android:name=".demo.fragment.ViewPager2Activity" />
        <activity android:name=".demo.fragment.AddFragmentActivity" />
        <activity android:name=".demo.manager.HiManagerActivity" />
        <activity android:name=".demo.banner.HiBannerDemoActivity" />
        <activity android:name=".demo.refresh.HiRefreshTextActivity" />
        <activity android:name=".demo.refresh.HiRefreshLottieActivity" />
        <activity android:name=".demo.tab.HiTabTopDemoActivity" />
        <activity android:name=".demo.tab.HiTabBottomDemoActivity" />
        <activity android:name=".demo.HiUiDemoActivity" />
        <activity android:name=".demo.log.HiLogDemoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>